/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["res.cloudinary.com", "unsplash.com", "cdn.example.com"], // add Cloudinary domain here
  },
};

export default nextConfig;
